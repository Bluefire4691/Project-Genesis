# Project Genesis — Python dependencies
#
# Core: no external dependencies required.
# Genesis runs on Python stdlib + SQLite (included with Python).
#
# Optional — audio output (speakers):
#   pip install pyttsx3
#
# Optional — microphone input:
#   pip install SpeechRecognition pyaudio
#
# Knowledge acquisition — WordNet dictionary (150k+ words, works offline):
#   First run: python -c "import nltk; nltk.download('wordnet'); nltk.download('omw-1.4')"
#   Or Genesis auto-downloads on first lookup if internet is available.
nltk>=3.8

# Development / testing:
pytest>=7.0

# Web browsing — autonomous knowledge acquisition from the open web:
#   pip install playwright trafilatura ddgs
#   python -m playwright install chromium   (downloads ~120 MB Chromium)
# Falls back to requests-only mode if playwright is not installed.
# Falls back gracefully (no web source) if all three are unavailable.
playwright>=1.40
trafilatura>=2.0
ddgs>=9.0
