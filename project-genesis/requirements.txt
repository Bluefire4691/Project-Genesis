# Project Genesis — dependencies
# Run install.bat (Windows) or install.sh (Mac/Linux) — do not pip install manually.

# Knowledge acquisition
nltk>=3.8
psutil>=5.9

# Web browsing
playwright>=1.40
trafilatura>=2.0
ddgs>=9.0

# HTTP (stdlib on most systems, explicit here for clarity)
requests>=2.28

# Terminal UI (clean single-window chat + live status)
rich>=13.0

# Development / testing
pytest>=7.0
